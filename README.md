# TravelGuide - Информационный сайт о путешествиях

Проект создан на Next.js 15.5.5 с использованием SQLite и Drizzle ORM.

## Технологический стек

- **Frontend/Backend**: Next.js 15.5.5 (App Router)
- **Язык**: TypeScript 5
- **База данных**: SQLite с Drizzle ORM
- **Аутентификация**: NextAuth.js 4.24.11
- **State Management**: Zustand 5.0.8, TanStack Query 5
- **UI**: Tailwind CSS 4
- **Валидация**: Zod 4.1.12

## Установка и запуск

1. Установите зависимости:

```bash
npm install
```

2. Создайте файл `.env.local` с переменными окружения:

```env
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000
DATABASE_PATH=./database.db
```

**Примечание:** Для отправки email в продакшене рекомендуется использовать nodemailer. В разработке email логируется в консоль. См. раздел "Настройка отправки email" ниже.

3. Создайте базу данных и примените миграции:

```bash
# Быстрый способ - создание всех таблиц (рекомендуется)
npm run db:push
# При запросе подтверждения введите "Yes"

# Или используйте миграции:
npm run db:generate  # Генерация миграций на основе схемы
npm run db:migrate   # Применение миграций
```

**Важно:** Таблица `users` создается автоматически при первом запуске, но для всех остальных таблиц рекомендуется выполнить `npm run db:push`.

4. Запустите сервер разработки:

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Работа с базой данных

### Команды Drizzle Kit

- `npm run db:generate` - Генерация миграций на основе схемы
- `npm run db:migrate` - Применение миграций
- `npm run db:push` - Быстрое создание/обновление таблиц (только для разработки)
- `npm run db:studio` - Открыть Drizzle Studio для просмотра данных
- `npm run db:seed` - Заполнение базы данных тестовыми данными (страны, города, достопримечательности, маршруты, статьи блога)

### Первоначальная настройка

1. Запустите `npm run db:seed` для создания таблиц и заполнения базы данных тестовыми данными
2. Тестовый пользователь будет создан автоматически:
   - Email: `admin@travelguide.com`
   - Password: `test123`

### Структура базы данных

Схема базы данных находится в `src/lib/db/schema.ts` и включает следующие таблицы:

- `users` - Пользователи
- `continents` - Континенты
- `countries` - Страны
- `cities` - Города
- `attractions` - Достопримечательности
- `routes` - Маршруты
- `routePoints` - Точки маршрута
- `reviews` - Отзывы
- `ratings` - Рейтинги
- `travelNotes` - Путевые заметки
- `visitedPlaces` - Посещенные места
- `wishlists` - Желаемые направления
- `articles` - Статьи блога

## Структура проекта

```
src/
├── app/              # Next.js App Router страницы и API routes
├── lib/
│   ├── auth.ts      # Конфигурация NextAuth
│   └── db/          # Работа с базой данных
│       ├── index.ts # Подключение к БД и вспомогательные функции
│       ├── schema.ts # Схема базы данных (Drizzle ORM)
│       └── init.ts  # Инициализация БД
```

## Настройка отправки email

Для работы функции сброса пароля используется EmailJS. В разработке email логируется в консоль, если EmailJS не настроен.

### Настройка EmailJS

1. **Зарегистрируйтесь на EmailJS:**
   - Перейдите на [EmailJS.com](https://www.emailjs.com/)
   - Создайте бесплатный аккаунт

2. **Создайте Email Service:**
   - В панели управления перейдите в "Email Services"
   - Добавьте новый сервис (Gmail, Outlook и т.д.)
   - Следуйте инструкциям для подключения вашего email провайдера

3. **Создайте Email Template:**
   - Перейдите в "Email Templates"
   - Создайте новый шаблон
   - **Важно:** В настройках шаблона включите "HTML Content"
   - Используйте следующие переменные в шаблоне:
     - `{{to_email}}` - email получателя (обязательно)
     - `{{subject}}` - тема письма (обязательно)
     - `{{message_html}}` - HTML содержимое письма (для HTML писем)
     - `{{message_text}}` - текстовое содержимое письма (опционально)
   
   **Пример содержимого шаблона:**
   ```
   Тема: {{subject}}
   
   {{message_html}}
   ```
   
   Или для простого текста:
   ```
   Тема: {{subject}}
   
   {{message_text}}
   ```

4. **Получите ключи:**
   - **Public Key (User ID)**: Находится в "Account" → "General"
   - **Service ID**: ID вашего email сервиса
   - **Template ID**: ID вашего шаблона

5. **Добавьте в `.env.local`:**
```env
EMAILJS_PUBLIC_KEY=your-public-key-here
EMAILJS_SERVICE_ID=your-service-id-here
EMAILJS_TEMPLATE_ID=your-template-id-here
```

### Пример шаблона EmailJS

В шаблоне EmailJS используйте:
```
Тема: {{subject}}

{{message_html}}
```

Или для простого текстового шаблона:
```
Тема: {{subject}}

{{message_text}}
```

### Проверка настройки

1. **Проверьте переменные окружения:**
   ```bash
   npx tsx scripts/check-emailjs.ts
   ```

2. **Перезапустите сервер** после добавления переменных в `.env.local`:
   ```bash
   # Остановите сервер (Ctrl+C)
   npm run dev
   ```

3. **Проверьте консоль** при запросе сброса пароля:
   - `✅ Email успешно отправлен на: email@example.com` - если все настроено правильно
   - `📧 Email отправка (EmailJS не настроен...)` - если EmailJS не настроен
   - `❌ Ошибка EmailJS API` - если есть проблема с ключами или шаблоном

### Важные моменты

- **Формат ключей:**
  - `EMAILJS_PUBLIC_KEY` - длинная строка (обычно 20+ символов)
  - `EMAILJS_SERVICE_ID` - должен начинаться с `service_`
  - `EMAILJS_TEMPLATE_ID` - должен начинаться с `template_`

- **Шаблон EmailJS должен содержать:**
  - `{{to_email}}` - email получателя
  - `{{subject}}` - тема письма
  - `{{message_html}}` - HTML содержимое (или `{{message_text}}` для текста)

- **После изменения `.env.local` обязательно перезапустите сервер!**

### Бесплатный лимит EmailJS

- Бесплатный план: 200 писем/месяц
- Для продакшена рассмотрите платные планы или альтернативы (SendGrid, AWS SES, Resend)

## Сброс пароля

Система сброса пароля реализована с использованием:
- Токенов сброса пароля (действительны 1 час)
- Серверной отправки email
- Валидации паролей (совместимо с подходами Passport.js)
- Безопасного хеширования паролей через bcrypt

Утилиты для работы с паролями находятся в `src/lib/password.ts` и совместимы с подходами Passport.js для аутентификации.

## Дополнительная информация

- База данных SQLite создается автоматически в файле `database.db` в корне проекта
- Все миграции хранятся в папке `drizzle/`
- Для продакшена рекомендуется использовать PostgreSQL или другую production-ready БД
